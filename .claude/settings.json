{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "[ \"$(git branch --show-current)\" != \"main\" ] || { echo '{\"decision\": \"block\", \"reason\": \"Cannot edit files on main branch. Create a feature branch first.\"}'; exit 2; }",
            "timeout": 5
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(cat | jq -r '.file_path // empty'); if [ -n \"$FILE\" ] && command -v npx &>/dev/null && [ -f \"$(git rev-parse --show-toplevel 2>/dev/null)/package.json\" ]; then npx prettier --write \"$FILE\" 2>/dev/null; fi",
            "timeout": 15
          },
          {
            "type": "command",
            "command": "FILE=$(cat | jq -r '.file_path // empty'); if [ -n \"$FILE\" ] && [[ \"$FILE\" == *.ts || \"$FILE\" == *.tsx ]]; then npx tsc --noEmit --pretty 2>&1 | head -20; fi",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
